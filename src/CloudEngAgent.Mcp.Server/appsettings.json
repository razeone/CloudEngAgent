{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "KeyVault": { "Uri": "" },
  "Mcp": {
    "SqlServer": {
      // Map a logical database name (used as the `database` argument to MCP
      // tools) to a SQL Server ADO.NET connection string. The first entry is
      // the default when callers omit `database`. Example:
      //   "default": "Server=localhost,1433;Database=master;User Id=sa;Password=...;TrustServerCertificate=true"
      "ConnectionStrings": {
        "default": ""
      }
    }
  }
}
